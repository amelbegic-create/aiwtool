// NOTE:
// This file exists for backwards compatibility with older imports in the codebase.
// Do NOT define auth options here. The single source of truth is `lib/auth.ts`.

export { authOptions, getSession } from "@/lib/auth";
